{
  "permissions": {
    "defaultMode": "bypassPermissions",
    "deny": ["Read(tsconfig.json)", "Bash(vp dev)"]
  },
  "enableAllProjectMcpServers": true,
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "if jq -r '.tool_input.file_path | select(endswith(\".ts\") or endswith(\".tsx\"))' | rg -q .; then vp run check; fi"
          },
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path | select(endswith(\".ts\") or endswith(\".tsx\"))' | xargs -r vp fmt --write"
          },
          {
            "type": "command",
            "command": "if jq -r '.tool_input.file_path | select(endswith(\".ts\") or endswith(\".tsx\"))' | rg -q .; then vp test; fi"
          }
        ]
      }
    ]
  }
}
